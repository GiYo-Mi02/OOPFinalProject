# Rename this file to ".env" and fill with real credentials before running the API.
# Values below are placeholders; replace everything after the equals sign.

NODE_ENV=development
PORT=4000

# Supabase project configuration
SUPABASE_URL=https://your-project-ref.supabase.co
SUPABASE_ANON_KEY=your-public-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Redis connection string (e.g., from Upstash or self-managed instance)
REDIS_URL=redis://default:password@localhost:6379

# Application-level secret for signing session tokens
APP_JWT_SECRET=replace-with-strong-hex-string

# Email provider configuration for OTP delivery (REQUIRED for authentication)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@umak.edu.ph
SMTP_PASS=your-app-password
EMAIL_FROM="UMak eBallot" <noreply@umak.edu.ph>

# Performance tuning (optional - defaults are set for moderate load)
# Increase these for high-concurrency scenarios
REQUEST_TIMEOUT=30000
# Request timeout in milliseconds (default: 30000 = 30 seconds)
BODY_SIZE_LIMIT=50mb
# Maximum request body size (default: 50mb)
REDIS_CACHE_TTL=30
# Redis cache TTL in seconds (default: 30)
